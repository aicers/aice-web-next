# Validates against schemas/giganto.graphql.
#
# FileCreateEvent (File create) — Relay cursor pagination over Giganto's
# FileCreateEventConnection. One of the 14 Sysmon / Windows endpoint
# operations (E2); the node selection is the full FileCreateEvent field set,
# kept in lockstep with SYSMON_RECORD_DEFS in records.ts.
query FileCreateEvents(
  $filter: NetworkFilter!
  $first: Int
  $after: String
  $last: Int
  $before: String
) {
  fileCreateEvents(
    filter: $filter
    first: $first
    after: $after
    last: $last
    before: $before
  ) {
    pageInfo {
      hasPreviousPage
      hasNextPage
      startCursor
      endCursor
    }
    edges {
      node {
        time
        agentName
        agentId
        processGuid
        processId
        image
        targetFilename
        creationUtcTime
        user
      }
      cursor
    }
  }
}
